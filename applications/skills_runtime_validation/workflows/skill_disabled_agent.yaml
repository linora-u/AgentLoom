name: "skill_runtime_disabled"
agent_runtime: smolagents
description: "Validate that removing the Skill tool still permits an ordinary real-model run."

model_type: "powerful"
toolsets: []
todo:
  mode: "off"
max_steps: 3

workflow: |
  Return exactly `SKILL_TOOL_DISABLED_OK`. Do not add punctuation or explanation.

worker_agents: []
