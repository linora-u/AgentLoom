name: inspect_note
agent_runtime: pi
description: Read the requested release note using your native read tool.
task: Read the file in query using read.
toolsets: []
tools:
- name: read
input_schema:
  type: object
  properties:
    query:
      type: string
      description: Path of the requested note.
  required:
  - query
  additionalProperties: false
