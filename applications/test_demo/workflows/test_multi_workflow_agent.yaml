name: "test_multi_workflow_agent"
agent_runtime: smolagents
description: |
  Demo agent for a multi-stage workflow prompt.

model_type: "powerful"
runtime_options:
  max_steps: 3
  smart_summary: false

task: |
  # Multi-stage workflow

  Complete both instructions in one Agent run:

  1. Retain the marker `FIRST_WORKFLOW_DONE marker=ALPHA17`.
  2. Return exactly: `SECOND_WORKFLOW_DONE marker=BETA29`.

tools: []
worker_agents: []
toolsets: []
