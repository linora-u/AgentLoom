checkpoint:
  enabled: false

skills: []

self_learning:
  enabled: true
  review:
    enabled: true
    application:
      review_model: "summary"
      trigger:
        mode: after_run
      approval:
        fact: manual
        experience: manual
    project:
      review_model: "summary"
      trigger:
        mode: manual
      approval:
        fact: manual
        experience: manual
