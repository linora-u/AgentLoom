checkpoint:
  enabled: false


self_learning:
  enabled: true
  review:
    enabled: true
    application:
      review_model: "summary"
      trigger:
        mode: after_run
      approval:
        fact: auto
        experience: auto
    project:
      review_model: "summary"
      trigger:
        mode: manual
      approval:
        fact: auto
        experience: auto
